/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "localhost",
      "picsum.photos",
      "six-favoritephoto-4team-be.onrender.com",
    ],
  },
};

export default nextConfig;
